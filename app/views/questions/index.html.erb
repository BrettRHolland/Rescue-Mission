<div class="row">
  <div class="col">

    <h1>Questions</h1>

    <% @questions.each do |question| %>
      <div class="card my-4">
        <div class="card-body">
          <h3 class="card-title"><%= question.title %></h3>
          <p class="card-text"><%= truncate(question.description, length: 200) %></p>
          <%= link_to "More", question_answers_path(question), class: "btn btn-secondary btn-sm" %>
        </div>
      </div>
    <% end %>

    <%= link_to 'New question', new_question_path, class: "btn btn-primary" %>

  </div>
</div>
